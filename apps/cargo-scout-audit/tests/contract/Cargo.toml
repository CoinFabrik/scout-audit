[package]
edition = "2021"
name = "almost-empty-contract"
version = "0.1.0"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = { version = "21.6.0" }

[dev-dependencies]
soroban-sdk = { version = "21.6.0", features = ["testutils"] }

[features]
testutils = ["soroban-sdk/testutils"]
